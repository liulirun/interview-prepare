I want to 12 programming questions:  for Stream, aim for get familiar with build-in functions + type conversion.
1. includes answers in code. 
2. for any convertion, print the converted type for me to easy understand
3. also a list of seperate questions, so I can copy paste

## Question2 : summarize 
Here are the rules used to structure the markdown tables for the methods and conversions we discussed. You can use these as a template for any future Java classes you study.

1\. Conversion Tables (Type Transformation)

- **Column 1: From**: The starting data source (e.g., `Array`, `Stream`).
- **Column 2: Example**: The specific syntax used to perform the change.
- **Column 3: When to use this**: The practical scenario where this conversion is necessary.
- **Column 4: Protip**: A "guru" secret or common pitfall to avoid (e.g., performance or null safety).
- **Goal**: To provide a **cheatsheet** for moving data between different Java structures.


2\. Public Methods (API Toolkit)

- **Column 1: Method**: The exact name (e.g., `put`, `sort`).
- **Column 2: Description**: A "plain English" explanation of what it does.
- **Column 3: Return Type**: Shows what data the method "gives back" (e.g., `void`, `boolean`, `E`).
- **Column 4: Example**: A concise code snippet showing the method in action.
- **Goal**: To show you **what** you can do with the class in your daily code.

3\. Private/Protected & Internal Methods (Behind the Scenes)

- **Column 1: Method**: The internal name (e.g., `resize`, `hash`).
- **Column 2: Description**: What internal logic it handles for the JVM.
- **Column 3: Category**: Identifies if it's for **Internal** logic, **Performance**, or **Memory**.
- **Column 4: Usage Context**: Explains **when** the JVM triggers this method automatically.
- **Goal**: To explain **how** the class works under the hood so you can write better-optimized code.


**Pro-Tip for future usage:** Use these tables to quickly spot **Type Mismatches**—like the `int[]` vs `Integer[]` issue we fixed earlier—before you even start typing.

Do you have another **Java class** (like `Scanner` or `Optional`) you'd like me to apply these rules to next?