{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "JavaScript: Debug Current File",
      "type": "pwa-node",
      "request": "launch",
      "cwd": "${workspaceFolder}",
      "program": "${file}",
      "skipFiles": ["<node_internals>/**"]
    }
  ]
}
